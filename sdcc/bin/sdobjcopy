#!/bin/sh

EXEEXT=
bin=sdobjcopy${EXEEXT}
abs_top_builddir=/home/loan/punxa_6502/sdcc
path=${abs_top_builddir}/support/sdbinutils/binutils

# use different path, if sth. there (e.g. on cygwin)
if [ -f ${path}/.libs/$bin ]; then \
	path=$path/.libs
fi

$path/$bin "$@"
